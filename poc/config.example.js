// Copia questo file in "config.js" e inserisci i valori del tuo progetto Supabase.
// config.js è escluso da git (vedi .gitignore) e non deve mai essere committato,
// anche se contiene la sola anon key (pensata per l'uso pubblico lato client,
// protetta dalle policy di Row Level Security definite in supabase/schema.sql).

export const SUPABASE_URL = 'https://YOUR-PROJECT-ID.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
